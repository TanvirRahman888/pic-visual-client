

const SubTitle = ({title, serviceDetails}) => {
    return (
        <div>
            <h2>{title}</h2>
            <hr />
            <p>{serviceDetails}</p>
        </div>
    );
};

export default SubTitle;